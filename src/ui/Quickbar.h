#pragma once

// The Quickbar window — a separate, smaller HUD-style window with one
// favorites category visible at a time. Registered with Nexus as its own
// renderer in AddonLoad.

void QuickbarRender();
