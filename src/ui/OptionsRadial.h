#pragma once

// Options "RadialMenus" tab: export a favorites category as a RadialMenus wheel
// (staged under addons/emot3/radials/<slug>/), manage exported wheels, and - in a
// +plus build - deploy them into RadialMenus' folder. See data/RadialExports.h,
// core/RadialExport.h, and the v1.3 plan (Feature 2b). Registered in the Options
// tab dispatcher (ui/Options.cpp).

void RenderRadialTab();
